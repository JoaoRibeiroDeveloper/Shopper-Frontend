import * as Styled from './styles';

const Header = () => {
  return (
    <Styled.Header>
      <Styled.Container>
        <Styled.Logo src="/logo.webp" />
      </Styled.Container>
    </Styled.Header>
  );
};

export default Header;
